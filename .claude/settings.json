{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npm run type-check && npm run lint && npm run format && npm run build",
            "statusMessage": "Running type-check, lint, format, and build..."
          }
        ]
      }
    ]
  }
}
